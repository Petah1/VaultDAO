/**
 * Storage Module
 *
 * Provides abstractions and implementations for persisting indexed
 * backend state. Supports multiple storage backends (in-memory, SQLite,
 * Postgres, etc).
 */

export * from "./storage.adapter.js";
